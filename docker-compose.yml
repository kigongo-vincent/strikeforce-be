services:
  database: 
    image: postgres 
    container_name: strikeforce3

    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=Admin@2025
      - POSTGRES_DB=strikeforce_db3

    ports:
      - "5432:5432"  